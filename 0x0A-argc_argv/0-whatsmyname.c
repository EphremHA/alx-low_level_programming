#include <stdio.h>

/**
 * main - prints program name
 * @argc: ardument count
 * @argv: string vector
 *
 * Return: none
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[0]);
	return (argc);
}
